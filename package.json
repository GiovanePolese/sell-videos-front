{
  "name": "sell-videos-front-monorepo",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "generate": "turbo run generate"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
